fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.9
pydantic-settings>=2.5
celery[redis]>=5.4
redis>=5.0
pypdf>=5.0
pdfminer.six>=20240706
python-docx>=1.1
openpyxl>=3.1
# --- Phase 2: vision / audio processing node ---
opencv-python-headless>=4.10
Pillow>=10.0
numpy>=1.26
faster-whisper>=1.0          # optional at runtime: lazily imported, graceful degradation
# optional: easyocr>=1.7     # OCR engine; lazily imported, graceful degradation when absent
# --- Phase 3: tabular / telemetry / vector indexer ---
polars>=1.0                  # streaming tabular engine (CSV/XLSX/JSON)
qdrant-client>=1.10          # vector DB client; lazily imported, offline spool fallback
# optional: sentence-transformers>=3.0  # MiniLM embeddings; hash fallback used when absent
pytest>=8.3
httpx>=0.27
